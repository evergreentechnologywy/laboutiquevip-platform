#!/usr/bin/env node
/**
 * RETIRED (2026-08-15 owner directive).
 * UltraGFE scrape is permanently disabled for laboutiquevip.net.
 * Allowed catalog sources: eros, tryst only.
 * External scrapers must POST to POST /api/v1/catalog/ingest (service JWT).
 */
console.error(
  "[import-ultragfe] RETIRED — UltraGFE scrape is disabled. Use eros/tryst via POST /api/v1/catalog/ingest.",
);
process.exit(2);
